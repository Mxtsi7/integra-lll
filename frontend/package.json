{
  "name": "ojoalgasto-frontend",
  "private": true,
  "description": "Workspace de los clientes: web, movil y el codigo que comparten. Ver README.md.",
  "workspaces": [
    "shared",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
