{
  "name": "@ojoalgasto/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Lo que la web y la app movil comparten: cliente de API, tipos, formato y calculos. Sin componentes ni estilos.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
